(function($){    $(document).ready(function(){            $('.minicolors').each(function() {                $(this).minicolors({                    control: $(this).attr('data-control') || 'hue',                    position: $(this).attr('data-position') || 'right',                    theme: 'bootstrap',                });            });    })    if($.fn.colorpicker){        $('.colorpicker-enable:not(.color-processed)').once('color').colorpicker();    }}(jQuery));